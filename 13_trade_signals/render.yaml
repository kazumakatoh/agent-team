services:
  - type: web
    name: trade-signals
    runtime: python
    rootDir: 13_trade_signals
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
